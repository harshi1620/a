import React from 'react'

function Differences() {
  return (
    <div>
{/* Props are used to pass data from a parent component to a child component.
State is used to store and update data within a component.
Props are read-only and cannot be changed by the receiving component.
State is mutable and can be updated using useState (functional components) or setState (class components).
Updating state causes the component to re-render. Props change only when the parent passes new values. */}
    </div>
  )
}

export default Differences
